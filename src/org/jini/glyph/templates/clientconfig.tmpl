import net.jini.discovery.LookupDiscovery;
import net.jini.lookup.entry.*;
import net.jini.core.entry.*;

$[packageName].$[className]{
	   lookupGroups = $[group];
       lookupLocators = null;
       lookupAttributes = null;
       lookupServiceClass = new Class[]{$[castClassName].class};
       lookupFilter = null;
} 	